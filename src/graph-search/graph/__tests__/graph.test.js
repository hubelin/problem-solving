import { Graph } from '../graph';

describe('Graph', () => {
  it('graph is defined', () => {
    expect(Graph).toBeDefined();
  });
  // it('new stack has size of 0', () => {
  //   let s = new Stack();
  //   expect(s.size()).toBe(0);
  // });
});
